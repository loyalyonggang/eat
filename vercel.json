{
  "buildCommand": "npx tsx scripts/convert.ts && pnpm run generate",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
